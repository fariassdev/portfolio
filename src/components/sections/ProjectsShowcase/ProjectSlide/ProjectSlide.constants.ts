// Clip Path Configuration
export const SLIDE_CLIP_MAX_OFFSET = 25;
export const CLIP_PATH_CENTER = 50;
export const CLIP_PATH_WIDTH = 24;
export const CLIP_PATH_TILT_FACTOR = 8;

// Animation Timing Constants (as percentages of reveal/blur phases)
export const SWEEP_START_PERCENT_MOBILE = 0.1; // Sweep text begins early on mobile (10% reveal)
export const SWEEP_START_PERCENT_DESKTOP = 0.5; // Sweep text begins later on desktop (50% reveal)
export const FADE_DURATION_PERCENT_MOBILE = 0.1; // Fade in/out happens in first 10% of reveal/blur on mobile
export const FADE_DURATION_PERCENT_DESKTOP = 0.4; // Fade in/out happens in first 40% of reveal/blur on desktop
